"""Geometry builders for the GCR model.

Pure mathematics (no OpenMC): hexmaths.
OpenMC cell builders: cavity, tie_rods, moderator, overlaps.

Import the submodules explicitly, e.g.:
    from gcr.geometry.cavity import build_cavity
"""
